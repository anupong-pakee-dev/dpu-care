# MongoDB connection string
MONGO_URL=

# JWT signing (login / email verification / password reset tokens)
SECRET_KEY=
ALGORITHM=HS256

# Frontend origin(s) allowed by CORS, comma-separated if more than one
CLIENT_URL=http://localhost:5173

# OpenAI
MODEL_NAME=gpt-4o-mini
OPENAI_API_KEY=

# SMTP (sends the email verification code)
SMTP_USER=
SMTP_SERVER=
SMTP_PORT=587
SMTP_PASSWORD=

# Declared in config.py but not currently used anywhere in the backend
CLIENT_ID=
